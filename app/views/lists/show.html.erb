<p id="notice"><%= notice %></p>

<p>
  <strong>Epic:</strong>
  <%= @list.epic %>
</p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>


<%= link_to 'New Card', new_card_path(list_id: @list.id, epic_id: @list.epic.id) %>
